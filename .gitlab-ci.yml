stages:
  - publish
  - deliver

mirror:
  stage: publish
  script:
    - setsid ./ci/mirror.sh
  only:
    - /^(main.*)$/
  tags:
    - mirror

deliver_python:
  stage: deliver
  script:
    - setsid ./ci/docker-deliver.sh "gcp-cloud-identity-debug-py" "1.0" "python"
  only:
    refs:
      - /^(main.*)$/
    changes:
      - .gitlab-ci.yml
      - ci/docker-deliver.sh
      - python/*
  tags:
    - imagesbuilder
